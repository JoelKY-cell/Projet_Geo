#  Architecture Frontend - Vue d'Ensemble

##  Schéma de l'Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         NAVIGATEUR                               │
│                    http://localhost:5173                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         App.jsx                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Router + ThemeProvider + CssBaseline                     │  │
│  │  - Gestion authentification                               │  │
│  │  - Récupération du rôle utilisateur                       │  │
│  │  - Affichage du dashboard selon le rôle                   │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                ▼                           ▼
    ┌───────────────────┐       ┌───────────────────┐
    │  Non Authentifié  │       │   Authentifié     │
    └───────────────────┘       └───────────────────┘
                │                           │
                ▼                           ▼
    ┌───────────────────┐       ┌───────────────────┐
    │   Login.jsx       │       │   Navbar.jsx      │
    │                   │       │  (filtré par rôle)│
    │ - Formulaire      │       └───────────────────┘
    │ - Validation      │                   │
    │ - Récup. rôle     │                   ▼
    │ - Stockage JWT    │       ┌───────────────────┐
    └───────────────────┘       │   Routes          │
                                └───────────────────┘
                                          │
                    ┌─────────────────────┼─────────────────────┐
                    │                     │                     │
                    ▼                     ▼                     ▼
        ┌───────────────────┐ ┌───────────────────┐ ┌───────────────────┐
        │   Dashboard       │ │   VehicleMap      │ │   Reports         │
        │   (selon rôle)    │ │                   │ │   (selon rôle)    │
        └───────────────────┘ └───────────────────┘ └───────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Admin        │ │ Supervisor   │ │ User         │
│ Dashboard    │ │ Dashboard    │ │ Dashboard    │
│              │ │              │ │              │
│ 8 stats      │ │ 4 stats      │ │ 4 stats      │
│ 2 tableaux   │ │ 2 sections   │ │ 1 section    │
└──────────────┘ └──────────────┘ └──────────────┘
        │                │                │
        └────────────────┼────────────────┘
                         │
                         ▼
              ┌───────────────────┐
              │   services/api.js │
              │                   │
              │ - axios config    │
              │ - JWT interceptor │
              │ - API endpoints   │
              └───────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND DJANGO                                │
│                  http://localhost:8000                           │
│                                                                  │
│  /api/auth/login/     → JWT Token                               │
│  /api/auth/profile/   → User Role                               │
│  /api/dashboard/      → Stats (personnalisées par rôle)         │
│  /api/vehicles/       → Véhicules                               │
│  /api/tracking/       → Positions GPS                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flux d'Authentification

```
┌──────────┐
│ Utilisateur │
└──────────┘
     │
     │ 1. Entre username/password
     ▼
┌──────────────┐
│  Login.jsx   │
└──────────────┘
     │
     │ 2. POST /api/auth/login/
     ▼
┌──────────────┐
│   Backend    │ ──────► 3. Retourne JWT token
└──────────────┘
     │
     │ 4. Stocke token dans localStorage
     ▼
┌──────────────┐
│  Login.jsx   │
└──────────────┘
     │
     │ 5. GET /api/auth/profile/
     ▼
┌──────────────┐
│   Backend    │ ──────► 6. Retourne {role: 'admin'}
└──────────────┘
     │
     │ 7. setUserRole('admin')
     ▼
┌──────────────┐
│   App.jsx    │
└──────────────┘
     │
     │ 8. getDashboard() → AdminDashboard
     ▼
┌──────────────┐
│ AdminDashboard│
└──────────────┘
```

---

## 🎯 Flux de Données Dashboard

```
┌──────────────────┐
│ AdminDashboard   │
│ SupervisorDash   │
│ UserDashboard    │
└──────────────────┘
         │
         │ useEffect(() => fetchStats())
         ▼
┌──────────────────┐
│  dashboardAPI    │
│  .getStats()     │
└──────────────────┘
         │
         │ GET /api/dashboard/
         │ Headers: { Authorization: 'Bearer <token>' }
         ▼
┌──────────────────┐
│  Backend         │
│  core/views.py   │
└──────────────────┘
         │
         │ Vérifie le rôle de l'utilisateur
         │
    ┌────┴────┬────────────┐
    │         │            │
    ▼         ▼            ▼
┌──────┐ ┌──────┐ ┌──────────┐
│ Admin│ │ Super│ │ User     │
│ Stats│ │ Stats│ │ Stats    │
└──────┘ └──────┘ └──────────┘
    │         │            │
    └────┬────┴────────────┘
         │
         │ Retourne JSON avec stats
         ▼
┌──────────────────┐
│  Dashboard       │
│  setStats(data)  │
└──────────────────┘
         │
         │ Affiche les cartes statistiques
         ▼
┌──────────────────┐
│  StatCard        │
│  Components      │
└──────────────────┘
```

---

## 🔐 Système de Permissions

```
┌─────────────────────────────────────────────────────────────┐
│                    PERMISSIONS PAR RÔLE                      │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐
│ ADMIN        │
│ (admin)      │
└──────────────┘
      │
      ├─► Dashboard: AdminDashboard (8 stats + 2 tableaux)
      ├─► Menu: Tous les items visibles
      ├─► Véhicules: CRUD complet
      ├─► Rapports: Création + Lecture
      └─► Utilisateurs: Gestion complète

┌──────────────┐
│ SUPERVISEUR  │
│ (supervisor) │
└──────────────┘
      │
      ├─► Dashboard: SupervisorDashboard (4 stats + 2 sections)
      ├─► Menu: Dashboard, Carte, Véhicules, Rapports
      ├─► Véhicules: CRUD complet
      ├─► Rapports: Création + Lecture
      └─► Utilisateurs: Aucun accès

┌──────────────┐
│ UTILISATEUR  │
│ (user)       │
└──────────────┘
      │
      ├─► Dashboard: UserDashboard (4 stats + 1 section)
      ├─► Menu: Dashboard, Carte, Rapports
      ├─► Véhicules: Aucun accès
      ├─► Rapports: Lecture seule
      └─► Utilisateurs: Aucun accès
```

---

## 📊 Composants par Dashboard

```
┌─────────────────────────────────────────────────────────────┐
│                    ADMIN DASHBOARD                           │
├─────────────────────────────────────────────────────────────┤
│  Grid Container (spacing=3)                                  │
│  ┌────────────┬────────────┬────────────┬────────────┐      │
│  │ Véhicules  │ Conducteurs│  Alertes   │  Rapports  │      │
│  │ actifs     │ actifs     │ non traitées│ générés   │      │
│  │ (vert)     │ (bleu)     │ (orange)   │ (violet)   │      │
│  └────────────┴────────────┴────────────┴────────────┘      │
│  ┌────────────┬────────────┬────────────┐                   │
│  │ Distance   │ Vitesse    │ Événements │                   │
│  │ du jour    │ moyenne    │ carburant  │                   │
│  │ (bleu)     │ (bleu)     │ (rouge)    │                   │
│  └────────────┴────────────┴────────────┘                   │
│  ┌──────────────────────┬──────────────────────┐            │
│  │ Alertes récentes     │ Statistiques système │            │
│  │ (Tableau)            │ (Liste)              │            │
│  │ - Type               │ - Boîtiers GPS       │            │
│  │ - Véhicule           │ - Capteurs carburant │            │
│  │ - Sévérité           │ - Capteurs RFID      │            │
│  │                      │ - Utilisateurs       │            │
│  └──────────────────────┴──────────────────────┘            │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  SUPERVISOR DASHBOARD                        │
├─────────────────────────────────────────────────────────────┤
│  Grid Container (spacing=3)                                  │
│  ┌────────────┬────────────┬────────────┬────────────┐      │
│  │ Véhicules  │ Conducteurs│  Alertes   │ Distance   │      │
│  │ actifs     │            │            │ du jour    │      │
│  │ (vert)     │ (bleu)     │ (orange)   │ (bleu)     │      │
│  └────────────┴────────────┴────────────┴────────────┘      │
│  ┌──────────────────────┬──────────────────────┐            │
│  │ Alertes actives      │ Statistiques flotte  │            │
│  │ (Liste)              │ (Liste)              │            │
│  │ - Type + Badge       │ - Trajets            │            │
│  │ - Véhicule           │ - Vitesse moyenne    │            │
│  │                      │ - Événements carb.   │            │
│  │                      │ - Boîtiers GPS       │            │
│  └──────────────────────┴──────────────────────┘            │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    USER DASHBOARD                            │
├─────────────────────────────────────────────────────────────┤
│  Grid Container (spacing=3)                                  │
│  ┌────────────┬────────────┐                                │
│  │ Véhicules  │   Total    │                                │
│  │ actifs     │ véhicules  │                                │
│  │ (vert)     │ (bleu)     │                                │
│  └────────────┴────────────┘                                │
│  ┌────────────┬────────────┐                                │
│  │ Distance   │  Trajets   │                                │
│  │ aujourd'hui│  du jour   │                                │
│  │ (bleu)     │ (violet)   │                                │
│  └────────────┴────────────┘                                │
│  ┌──────────────────────────────────────┐                   │
│  │ Informations                         │                   │
│  │ Message d'aide à la navigation       │                   │
│  └──────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Actualisation Automatique

```
┌──────────────┐
│  Dashboard   │
│  Component   │
└──────────────┘
       │
       │ useEffect(() => {
       │   fetchStats();
       │   const interval = setInterval(fetchStats, 30000);
       │   return () => clearInterval(interval);
       │ }, []);
       │
       ▼
┌──────────────┐
│  fetchStats  │ ◄──────┐
└──────────────┘        │
       │                │
       │ API Call       │
       ▼                │
┌──────────────┐        │
│   Backend    │        │
└──────────────┘        │
       │                │
       │ Response       │
       ▼                │
┌──────────────┐        │
│  setStats()  │        │
└──────────────┘        │
       │                │
       │ Re-render      │
       ▼                │
┌──────────────┐        │
│  Dashboard   │        │
│  Updated     │        │
└──────────────┘        │
       │                │
       │ Wait 30s       │
       └────────────────┘
```

---

## 🎨 Hiérarchie des Composants

```
App.jsx
├── ThemeProvider
│   ├── CssBaseline
│   └── Router
│       ├── Login.jsx (non authentifié)
│       │   └── Form + TextField + Button
│       │
│       └── Authenticated Routes
│           ├── Navbar.jsx
│           │   └── AppBar + Toolbar + Buttons (filtrés)
│           │
│           ├── AdminDashboard.jsx (si role='admin')
│           │   ├── Container
│           │   └── Grid
│           │       ├── StatCard × 7
│           │       ├── Table (Alertes)
│           │       └── Card (Stats système)
│           │
│           ├── SupervisorDashboard.jsx (si role='supervisor')
│           │   ├── Container
│           │   └── Grid
│           │       ├── StatCard × 4
│           │       ├── List (Alertes)
│           │       └── Card (Stats flotte)
│           │
│           ├── UserDashboard.jsx (si role='user')
│           │   ├── Container
│           │   └── Grid
│           │       ├── StatCard × 4
│           │       └── Card (Informations)
│           │
│           ├── VehicleMap.jsx
│           ├── VehicleList.jsx
│           └── Reports.jsx
```

---

## 📡 API Endpoints Utilisés

```
┌─────────────────────────────────────────────────────────────┐
│                      API ENDPOINTS                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  POST /api/auth/login/                                       │
│  ├─ Body: { username, password }                            │
│  └─ Response: { access, refresh }                           │
│                                                              │
│  GET /api/auth/profile/                                      │
│  ├─ Headers: { Authorization: 'Bearer <token>' }            │
│  └─ Response: { id, username, email, role }                 │
│                                                              │
│  GET /api/dashboard/                                         │
│  ├─ Headers: { Authorization: 'Bearer <token>' }            │
│  └─ Response: { vehicles, drivers, alerts, ... }            │
│     (personnalisé selon le rôle)                            │
│                                                              │
│  GET /api/vehicles/                                          │
│  ├─ Headers: { Authorization: 'Bearer <token>' }            │
│  └─ Response: [ { id, license_plate, ... }, ... ]           │
│                                                              │
│  GET /api/tracking/positions/                                │
│  ├─ Headers: { Authorization: 'Bearer <token>' }            │
│  └─ Response: [ { vehicle, latitude, longitude, ... }, ... ]│
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## ✅ Points Clés de l'Architecture

1. **Séparation des rôles** - 3 dashboards distincts
2. **Authentification JWT** - Token stocké dans localStorage
3. **Permissions frontend** - Navigation filtrée par rôle
4. **Permissions backend** - API retourne données selon rôle
5. **Actualisation auto** - Toutes les 30 secondes
6. **Responsive** - Material-UI Grid avec breakpoints
7. **Modulaire** - Composants réutilisables (StatCard)
8. **Sécurisé** - JWT interceptor + validation backend

---

## 🎯 Avantages de cette Architecture

✅ **Maintenable** - Code organisé et modulaire
✅ **Sécurisé** - Double validation (frontend + backend)
✅ **Performant** - Actualisation intelligente
✅ **Évolutif** - Facile d'ajouter de nouveaux rôles
✅ **UX optimale** - Dashboard adapté à chaque utilisateur
✅ **Responsive** - Fonctionne sur tous les appareils
